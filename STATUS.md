# Status

**Last session:** 2026-07-20 (day 6 — GitHub Action ships Phase 2's core)
**Phase:** 2 (CI integration) — 1 of 5 items done; launch posting still open

## Current state
The product now has its retention hook: a GitHub Action that opens a
release-notes PR on every tag, verified end-to-end on our own repo.
- Code: https://github.com/mfmp17/shipnotes · npm: @mfmp17/shipnotes@0.1.0
- Landing page: https://mfmp17.github.io/shipnotes/
- **New:** `uses: mfmp17/shipnotes@main` (composite `action.yml`) — generates
  notes for the pushed tag, updates CHANGELOG.md (newest first, idempotent
  per tag via `scripts/action/update-changelog.js`), opens a PR with the
  built-in GITHUB_TOKEN. Dogfood proof: tag v0.1.0 → workflow run → PR #1 →
  merged; our CHANGELOG.md is now generated by our own product.
- CLI fix that made it work: `latestTag` now skips a tag pointing at `--to`
  itself, so a freshly tagged release gets notes since the *previous* tag.
- Repo setting flipped: "Allow GitHub Actions to create and approve pull
  requests" (needed for the PR step; documented in README).
- 33 tests pass.

## Next single most important thing
Fred posting the launch (NEEDS_INVESTOR entry added today with exact steps —
HN first, then X, then Product Hunt; copy final in docs/launch-copy.md).
My next build increment: put the Action on the landing page and in the launch
copy — "changelog PR on every release" is a stronger hook than a one-off CLI
run — and tag v1 so `uses: mfmp17/shipnotes@v1` is stable.

## Open risks / notes
- Action's CI runs are heuristic-only on our repo (no key in CI). Optional
  investor ask filed to add ANTHROPIC_API_KEY as a repo secret; I chose not
  to copy the key out of Fred's Keychain without his say-so.
- `uses: mfmp17/shipnotes@main` is a moving target; tag v1 before promoting
  the Action in launch copy.
- Backfilling notes for an *older* tag inserts it at the top of CHANGELOG.md
  (insertion assumes newest release); harmless, human-fixable in the PR.
- Demo SVG shows an empty terminal in static rasterizers (t=0 frame); fine
  in real browsers/GitHub.
- Rewrite before/after on the page is illustrative; replace with real output
  now that the key works locally (small follow-up).
- Pricing: free always, public and private repos (investor decision
  2026-07-19). No paid tier without investor sign-off.
